# HolyCrab CLI v0.4.3 清理与验收报告

## 2026-09-17 发布条件调整

用户明确授权本次改用与远端同步的 Generate 本地 main 对照，并沿用此前真人流程验收，不重复实际操作。该调整仅限 v0.4.3，不改变后续版本的原有检查条件。

- Generate 本地 main 已安全快进到 `3fb945d2d73dc1e4fa3a780d3cf90a66c3d7adfc`，与刚 fetch 的 origin/main 及 ls-remote 返回的真实远端 main 完全一致。
- 从该本地 main 创建干净 detached 源码副本，契约校验通过；原功能分支保持不变，没有 Generate 业务代码或生产变更。
- CI 本次仅核验版本限定的本地验收记录、main 提交一致性与 CLI/能力快照/验证器 SHA-256；源文件改变后必须重新校验，不能沿用旧哈希。
- 真人记录明确为 previously-accepted，testedAt=null，retestWaived=true，附用户确认；没有声称本轮执行了正式授权或上传。
- 调整后完整发现：332 项通过，0 失败，0 跳过，19.297 秒；新增测试拒绝缺少同步证据、输入文件改变及将例外沿用到 v0.4.4，并覆盖发布配置与 Draft 下载权限修复。
- 增加手动触发的官网入口烟测：Mac 与原生 PowerShell 5.1 下载官网安装器，先与正式 Release digest/源码逐字节核对，再隔离安装、检查版本/doctor/卸载帮助/20 个 Agent 工具登记与运行文件白名单，最后卸载。发布与官网同步后执行；不使用账号凭据或业务写入。
- 私库读取与实际验收不再是本次发布阻塞，其他自动化、安装、digest、Draft 与晋级门仍保留。下方 2026-09-16 的测试数及 pending 状态为历史记录，不是调整后的当前发布状态。

### 发布管线修复

CLI 已合并 main 并创建首次 v0.4.3 标签。标签运行的 11 个必需测试通过，安装器与 SHA256SUMS 已上传 Draft；资产验证因误用只支持已发布版本的 tag 查询接口返回 404，因此没有晋级 Latest。

另一个官网烟测工作流的 job.env 不允许使用 runner.temp，由专用 actionlint 复现。修复为运行器启动后的独立步骤设置临时目录，并为 Draft 校验改用经过认证的 Release 列表筛选唯一版本。补充仅限 v0.4.3 的手动恢复：重跑全部必需测试、验证原 Draft 文件摘要/字节、三平台真实安装后才发布，不创建、覆盖或重传原资产，也不改原标签。仅 CI、内部测试和报告变化，CLI/Skill/安装器字节不变。

恢复运行中摘要/字节门通过，三平台均在 Draft 下载阶段返回 release not found，未开始安装。GitHub 要求有 push 权限才可见 Draft；仅为原生 Draft 验证 job 使用内置的 contents:write，checkout 不持久化凭据，安装步骤不传 GH_TOKEN，不增加任何私库凭据或 Secret，该 job 也不编辑/上传 Release。发布门继续阻止不完整验收晋级。

## 初次验收结论（2026-09-16，历史）

清理与自动化验收已完成，版本保持 0.4.3。正式发布尚未完成：Generate 私库 CI 没有读取权限，真人授权与上传实际验收仍待用户确认。没有合并 CLI main、创建标签/Release、修改官网生产安装器或部署网站。

- 测试代码提交：`1626ea901df1ae89963c559382265c9c555cc592`。
- CLI PR：[AstroxNetwork/skills #3](https://github.com/AstroxNetwork/skills/pull/3)，继续保留 Draft。
- CI：[本轮执行记录](https://github.com/AstroxNetwork/skills/actions/runs/35116979371)，10 项通过、1 项权限失败；3 个仅标签触发的发布阶段未运行。
- 本地系统：macOS 27.0；Python 3.14.6；测试日期：2026-09-16。
- 完整发现：316 项通过，0 失败，0 跳过，20.718 秒。五语言官网联测使用隔离源码，未使用正式业务接口。
- 业务测试通过传输边界 Mock 执行；生成创建、授权创建、上传、改名、线上删除正式接口调用均为 0。本轮没有产生线上业务数据。

## 清理清单

- 删除 `holycrab/scripts/holycrab_api.py` 及其独立 polling/upload 入口；当前指引和 CI 不再引用。
- `auth set-key`、`credits estimate` 保留隐藏兼容，与 `setup`、`generate estimate` 共用实现；当前帮助和示例不推荐旧名称。
- 按配置目录节流 QR 过期扫描 5 秒，失败不阻断查询；授权终态立即删除 QR，不受扫描节流影响。
- 能力文件与模型索引在单次命令/MCP 调用内共享，返回深拷贝；结束或异常时释放。账号响应没有缓存。
- setup、账号状态、余额、online doctor 与 MCP account_get 共用账号边界；保留各入口错误、公开字段及环境变量覆盖语义。
- 删除未使用导入与重复 attempt ID 兜底，统一 MCP/安装后 Agent 引导规则；保留 onboarding 字段。
- 离线 doctor 的 CONFIGURED 仅表示本地已有 Key，不表示验证失败或账号已经连接。已有同对话有效验证时，Agent 不因离线 doctor 要求重复验证。
- 测试、Mock、fixture、烟测驱动和测试依赖迁到顶层 `tests/`；开发验证器迁到 `tools/`。

## 旧安全测试迁移

原旧辅助脚本 22 项测试中，10 项改为测试主 CLI；12 项重复覆盖由下列更完整的现有测试保留。另增加 21 项清理、指引、安装白名单及发布验收检查：307 − 22 + 10 + 21 = 316。

| 原覆盖 | 当前实际运行代码的覆盖 |
| --- | --- |
| 固定 API origin、同源带 token 跳转、跨源及用户名密码跳转阻断 | test_transport_security；test_cli 的自定义/配置 base URL 拒绝测试 |
| HTTPS 上传地址、危险协议及嵌入凭据拒绝 | test_transport_security |
| 所有上传重定向拒绝 | test_real_human.test_upload_redirect_handler_refuses_every_redirect |
| 递归预签名地址及打印脱敏、HTTP/业务失败退出 | test_transport_security |
| 普通字段签名 URL、嵌入错误文本、HTML 转义 URL、Key 脱敏 | test_cli.test_redacts_signed_result_urls_under_ordinary_response_keys 等 |
| polling 成功/失败/超时、不重提、状态去重 | test_command_matrix、test_feedback.test_task_wait_only_emits_changes_and_final_timeout |
| 中文 multipart 编码与登记、连续上传、登记 400 | test_cli.test_multipart_encoder_preserves_utf8_values、test_upload_registers_chinese_filename_as_multipart、test_consecutive_uploads_register_each_asset、test_registration_400_is_reported_without_returning_asset |
| 无任意 request 入口 | test_cli 参数错误覆盖；当前解析器能力清单及 test_public_guidance |

登记失败时，现实现保留已知资产 ID 用于恢复查询，并明确 state=failed；不会把它作为上传成功结果。这不是恢复旧脚本“丢弃已知 ID”的行为。

## 命令与 Agent 指引一致性

`docs/reports/v0.4.3-public-interface.json` 由实际 argparse/MCP schema 导出，包含 31 个叶子命令、20 个 MCP 工具和 196 条可执行 CLI 示例（仓库 71、官网 125）。31 个包含 2 个隐藏兼容入口，不是新增命令。

所有 CLI 命令在交互/非交互模式下验证成功、失败和 help 旁路维护；20 个 MCP 工具验证正常 fixture 结果、参数错误及协议 stdout 纯净。下面“通过”均指自动化/Mock，不代表正式业务写入验收。

| 命令 | 成功 fixture | 失败 fixture | 入口状态 |
| --- | --- | --- | --- |
| `holycrab setup` | 通过 | 通过 | 当前入口 |
| `holycrab auth set-key` | 通过 | 通过 | 隐藏兼容 |
| `holycrab auth status` | 通过 | 通过 | 当前入口 |
| `holycrab auth clear-key` | 通过 | 通过 | 当前入口 |
| `holycrab models list` | 通过 | 通过 | 当前入口 |
| `holycrab models show` | 通过 | 通过 | 当前入口 |
| `holycrab credits balance` | 通过 | 通过 | 当前入口 |
| `holycrab credits estimate` | 通过 | 通过 | 隐藏兼容 |
| `holycrab generate estimate` | 通过 | 通过 | 当前入口 |
| `holycrab generate create` | 通过 | 通过 | 当前入口 |
| `holycrab generate attempts list` | 通过 | 通过 | 当前入口 |
| `holycrab generate attempts get` | 通过 | 通过 | 当前入口 |
| `holycrab tasks get` | 通过 | 通过 | 当前入口 |
| `holycrab tasks list` | 通过 | 通过 | 当前入口 |
| `holycrab tasks wait` | 通过 | 通过 | 当前入口 |
| `holycrab download` | 通过 | 通过 | 当前入口 |
| `holycrab real-human start` | 通过 | 通过 | 当前入口 |
| `holycrab real-human get` | 通过 | 通过 | 当前入口 |
| `holycrab real-human wait` | 通过 | 通过 | 当前入口 |
| `holycrab real-human groups list` | 通过 | 通过 | 当前入口 |
| `holycrab real-human groups rename` | 通过 | 通过 | 当前入口 |
| `holycrab real-human groups delete` | 通过 | 通过 | 当前入口 |
| `holycrab real-human assets list` | 通过 | 通过 | 当前入口 |
| `holycrab real-human assets delete` | 通过 | 通过 | 当前入口 |
| `holycrab assets upload` | 通过 | 通过 | 当前入口 |
| `holycrab assets get` | 通过 | 通过 | 当前入口 |
| `holycrab assets wait` | 通过 | 通过 | 当前入口 |
| `holycrab mcp serve` | 通过 | 通过 | 当前入口 |
| `holycrab update` | 通过 | 通过 | 当前入口 |
| `holycrab doctor` | 通过 | 通过 | 当前入口 |
| `holycrab uninstall` | 通过 | 通过 | 当前入口 |

动态 nextAction 按 task、attempt、authorization、asset 状态构造，非空命令实际执行对应的同一隔离记录。不允许推荐直接重试写操作；缺少文件、目标或保存位置时 command=null，不编造参数。

真人指引要求：列出现有人物供用户选择；人物名称仅展示，不支持按姓名查询授权会话；授权成功只是人物分组可用，必须继续选择文件、完整预览、单独确认上传，再等待素材可用。授权、上传、付费分别确认。Skill 的独立 Agent 场景检查通过，未调用业务接口。

## 自动化与原生安装结果

| 验收项 | 结果/证据 |
| --- | --- |
| Linux/macOS/Windows，Python 3.10 与 3.14 | 6 个 CI job 全通过；各平台适用测试运行，POSIX 专属用例不声称在 Windows 原生执行 |
| 原生 Linux/macOS 安装及 MCP | 2 个 CI job 通过 |
| PowerShell 5.1、中文路径、UTF-8、当前/用户 PATH、MCP 修复、DPAPI、卸载 10 秒 | Windows 安装 job 通过；实际执行固定提交安装命令 |
| 官方 Skill validator | 通过 |
| QR 独立解码 | Linux Python 3.14 job 通过 |
| 安装、重复安装、旧版升级、失败回滚、默认卸载/重装、purge、用户状态保留 | 隔离 fixture/安装测试通过；不声称已测试未发布正式 Release 的在线 update |
| 上传确认/文件替换/特殊文件/部分失败/不确定结果 | Mock 通过，零正式上传 |
| 创建 unknown/中断/重复防护、下载地址及残片保护 | Mock 通过，零正式生成创建 |
| JSON/MCP 纯净、Key 脱敏、取消/中断反馈 | 通过 |
| Generate 本地只读契约 | 通过，源码 commit `99af1c2e42058391312f6a3bb6b5d860f26e0aa0` |
| Generate 远程 main 必需 CI | 失败，checkout 报 Repository not found；未执行契约比较，不得用本地通过代替 |
| 实际真人授权与上传 | 未测试，须用户验收 |
| Draft/正式 Release 安装、官网部署后新机安装、正式 update | 未测试；上游发布条件未满足 |

## 用户安装文件白名单

专用程序目录只允许以下文件，以及 Python 自动生成的当前 CLI 字节码：

- holycrab_cli.py
- installation.json（安装器生成，不保存 API Key）
- references/capabilities.json
- vendor/segno-1.6.6-py3-none-any.whl
- vendor/LICENSE.segno

另有一个系统对应的启动器。每个选择安装的 Skill 目录只包含：

- SKILL.md
- agents/openai.yaml
- references/capabilities.json

Unix 与原生 Windows 对实际安装目录执行白名单检查。运行代码不导入 tests/tools/unittest/pytest，不提供 Mock/fixture 切换开关。测试代码可保留在 GitHub 源码与自动源码归档中，但不属于用户安装内容。

### 候选发布资产 SHA-256

| 文件 | SHA-256 |
| --- | --- |
| install.sh | `1ae0af83a1cdf34b06bb39bfbc4d4dcdfa82efeecaa3e3b7e8d3e1f18411c500` |
| install.ps1 | `21391386f71095133c38f9ed587021e856af14710ffd665957b7fdc4f8edb571` |

CLI 核心与 Skill 哈希已同步到两套安装器。发布 job 从最终提交重新生成 SHA256SUMS。上面是候选源码资产校验值，不代表 GitHub Release digest 已完成验证。

## 官网准备情况

从官网最新 main 单独创建 worktree，法律文档修改保留在原工作区，未覆盖或混入。

- 五语言共 50 个 CLI 页面，指南总计 110 页；生成及一致性检查通过。
- 全部官网单元测试、TypeScript、Vite/Rspress 构建、HTML/Markdown、搜索/llms、本地化、SEO、分析脚本构建检查通过。
- 修正旧安装成功文字、方括号可复制语法、更新确认顺序，以及 v0.4.0 缺少 update/uninstall 时的升级说明。
- 桌面 1440、移动 390 浏览器抽查入口/Windows 安装/素材上传/真人流程：图片正常、页面无横向溢出、表格内部横向滚动、高级命令可展开收起；公开 Windows 命令复制的调用、内容匹配和写入完成均确认。
- 浏览器 readText 权限未授予，未读取用户剪贴板；复制通过包装原 writeText 收集布尔结果，仍执行真实写入。测试会话/预览服务检查后关闭。
- 原生安装示意图版本更新，不冒充真实新机截图或新人实测。
- 新官网安装器验证门拒绝 Draft/旧版/缺少或不匹配 digest/不匹配字节。实际检查 v0.4.3 返回 404，正确阻止部署。
- 官网 public/cli 仍保留上一套 v0.4.0 文件，没有用未发布测试文件覆盖。新版网站分支尚未部署；实际发布日期仍待 Release 发布后更新。

## 发布顺序已固化，当前不得继续

发布工作流保持所有必需 needs；只有标签触发，且手动验收记录明确通过后才创建 Draft。校验 GitHub digest/下载字节后，在三个原生系统安装 Draft 文件；全部成功才发布为 Latest。任何阶段失败，不覆盖既有资产，也不自动晋级。

当前 `docs/release-acceptance/v0.4.3.json` 明确 pending；运行 verify_release_acceptance 会拒绝发布。测试中使用的 passed fixture 不会写入真实验收记录。

继续发布前需要：

1. 为 CLI CI 配置有权只读 Generate 私库的 `GENERATE_CONTRACT_SSH_KEY`，重新跑必需契约检查。本轮没有创建、复制或上传凭据。
2. 用户完成真人授权→确认人物分组→选文件→完整预览→确认上传→等待可用的实际验收，并记录不含姓名、Key、私密链接或 QR 的结果。
3. 全部必需检查通过后，合并 CLI、创建新标签/Draft、校验并发布，再逐字节同步官网及更新真实发布日期，最后部署。
4. 完成官网新机 Mac/Windows 烟测与隔离旧版正式 update。尚未执行的项目不能提前打勾。
