interface:
  display_name: "HolyCrab Agent Tools"
  short_description: "查询能力、发起真人授权并安全使用素材创建 HolyCrab 生成任务"
  default_prompt: "使用 $holycrab 查询能力；需要真人素材时提供授权链接和二维码，等待本人验证与素材就绪，再估算积分，经我确认后只提交一次生成任务。重命名或删除前核对公开 ID；永久删除必须获得我对具体对象的明确确认。"
